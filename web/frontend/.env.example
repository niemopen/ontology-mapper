# No environment variables are required by the frontend.
#
# This app runs with a single fixed local user and has no external
# authentication provider. The Vite dev server proxies /api to the
# FastAPI backend on port 8000 (see vite.config).
