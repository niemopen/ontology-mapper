# Python
__pycache__/
.coverage
*.pyc
*.pyo
*.egg-info/
dist/
build/
.venv/

# Pipeline state and run artifacts (generated at runtime)
.mapper-runs/

# Agent session temp files
agent_sessions_tmp/

# Vector store — BGE embedding model weights only (~1.3 GB). The model
# auto-downloads via sentence-transformers on first use, so it is not
# committed. The FAISS indexes under specs/vector/indexes/ DO ship with
# the repo (see README) so a fresh clone can run without a rebuild.
src/ontology_mapper/specs/vector/model/

# Reference catalogs, summaries, type directories, NDR specs, and the
# cmf-xsd schemas under specs/ DO ship with the repo so a fresh clone
# has the static reference data the pipeline needs.


# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Claude Code local config
.claude/
